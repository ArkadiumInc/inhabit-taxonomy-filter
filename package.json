{
  "name": "inhabit-taxonomy-filter",
  "version": "1.2.0",
  "description": "taxonomy filter for inhabit",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run test",
    "test": "tsc && mocha ./dist/*.spec.js ./dist/**/*.spec.js",
    "test-watch": "concurrently \"tsc --watch\" \"ava dist/**/*.spec.js --watch\""
  },
  "git": "https://github.com/ArkadiumInc/inhabit-taxonomy-filter.git",
  "author": "wishmasterts",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.0.4",
    "@types/mocha": "^2.2.42",
    "concurrently": "^3.5.0",
    "cz-conventional-changelog": "^2.0.0",
    "mocha": "^3.5.0",
    "typescript": "^2.5.2"
  },
  "dependencies": {
    "chai": "^4.1.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
